# Let vs Const in JavaScript

`Let` and `const` were introduced in ES2015. These are special kind of keywords to declare variables in javascript.
It replaces `var` keyword in javascript

```ts
console.log("asfdasdf");
```

```cpp
#include<iostream>

int main(){

    return 0;
}
```
